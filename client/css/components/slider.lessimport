.iosSlider {
	.resetMarginPadding;

	margin: @gridColumnWidth 0;

	position: relative;
	top: 0;
	left: 0;
	overflow: hidden;
	width: 100%;
	margin-bottom: 0;
	.transition-property(height, min-height);
	.transition-duration(@transitionDuration);

	.slider {
		width: 100%;
		height: 100%;
	}
}