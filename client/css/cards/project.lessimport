.projectCard {
	z-index: 2;
	position: absolute;
	top: 0;
	left: 0;
	padding: @gridColumnWidth / 2;
	background-color: @background;
	background-size: cover;
	background-position: center;
	border: 1px solid @colourBlack;
	width: @gridColumnWidth * 14;
	.box-sizing(border-box);
	min-height: @gridColumnWidth * 9;
	height: @gridColumnWidth * 9;
	overflow: hidden;

	.transition-property(top, left, opacity, width);
	.transition-duration(@transitionDuration);
	.opacity(0.5);

	img {
		position: absolute;
		top: 0;
		left: 0;
		z-index: @secondaryLayer;
		.opacity(0);
		width: auto;
		height: 100%;
	}

	time, h2 {
		font-weight: 500;
		letter-spacing: 1px;
	}
	p {
		.resetMarginPadding;
		margin-top: @gridColumnWidth;
		font-style: italic;
		font-size: @standardFont - 2;
		letter-spacing: 1px; 
	}

	time,
	h2,
	p {
		position: relative;
		.transition-property(opacity);
		.transition-duration(@transitionDuration);
		z-index: @primaryLayer;
	}

	&.highlighted {
		.opacity(1.0);
	}

	&.animated {
		time, 
		h2, 
		p {
			opacity: 0.0;
		}
		img {
			.opacity(1.0);
			.transition-duration(@transitionDuration);
			.transition-delay(@transitionDuration);
			.transition-property(opacity);

			.animation(badtv 100ms linear);
			.animation-delay(@transitionDuration * 2);
		}
	}
}

@media @mobile {
	.projectCard {
		display: block;
		float: left;
		width: 100%;
		top: 0;
		left: 0;
		.box-sizing(border-box);
		border-left: 0;
		border-right: 0;
	}
}