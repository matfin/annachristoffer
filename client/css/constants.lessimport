// Fonts family
@standardFontFamily: "Helvetica Neue", Helvetica, Arial, "Lucida Grande", sans-serif;

// Font sizes
@baseFontSize: 16px;
@largeFont: @baseFontSize * 2;
@mediumFont: @baseFontSize * 1.5;
@smallFont: @baseFontSize * 0.75;

// Layout
@gridColumnWidth: 16px;
@maxWidth: @gridColumnWidth * 100;

// Colours 
@colourWhite: #fff;
@colourBlack: #000;
@background: rgba(red(@colourWhite), green(@colourWhite), blue(@colourWhite), 0.5);